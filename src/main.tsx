import React from 'react';
import ReactDOM from 'react-dom/client';
import DashboardArbitro from './pages';

import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DashboardArbitro />
  </React.StrictMode>
);
